apiVersion: v1
kind: Service
metadata:
  namespace: lb
  name: lb-{{ .Release.Name }}
spec:
  type: ExternalName
  externalName: {{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local
  ports:
    - port: 80
      name: http
      targetPort: 80
